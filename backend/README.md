# Taskflow Kanban Backend

## Day 1 Backend Setup

- Express server with CORS enabled
- MongoDB Atlas connection
- Task model (`title`, `description`, `status`)
- POST route `/api/tasks` to create tasks

## Day 2 Frontend-Backend Integration

- Connected frontend with backend API using Axios
- Updated `app.js` and backend routes
- Backend ready to serve tasks to frontend
